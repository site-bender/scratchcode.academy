---
import { Article } from "@sitebender/components";

export type Props = {
	title?: string | undefined | null;
};

const { title } = Astro.props;

const lessonTitle = (await Astro.slots.render("title")) || title;
---

<Article>
	<Fragment slot="title" set:html={lessonTitle} />
	<slot />
</Article>
